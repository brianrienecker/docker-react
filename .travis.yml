language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t brianrienecker/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true brianrienecker/docker-react npm run test